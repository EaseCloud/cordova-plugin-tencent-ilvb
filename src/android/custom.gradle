repositories {
    jcenter()
    flatDir {
        dirs 'aar'
    }
}

dependencies {
    compile(name: 'ilivesdk-1.5.0', ext: 'aar')
    compile(name: 'livesdk-1.1.1', ext: 'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}